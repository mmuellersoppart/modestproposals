#extend("base"):
    #export("content"):
        <ul>
            #for(proposal in homepageProposals):
                    <li>#(proposal.proposalTitle) by #(proposal.creatorUsername)</li>
            #endfor
        </ul>
    #endexport
#endextend

