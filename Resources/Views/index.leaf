#extend("base"):
    #export("content"):
        <!-- 3 -->
        #if(isLoggedIn):
            <ul>
                #for(proposal in homepageProposals):
                    <li>#(proposal.title)</li>
                #endfor
                
            </ul>
        #else:
            <h1>Please login</h1>
        #endif
    #endexport
#endextend

